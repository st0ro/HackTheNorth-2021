import React, { Component } from 'react';

class Form extends Component {
    render() {
        return (
            <div>This is where the form will go</div>
        );
    }
}

export default Form;